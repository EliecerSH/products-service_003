#Indicar aquí comienzan los servicios para Docker
services:
  ms-inventario:
    build: .
    container_name: ms-Producto
    ports:
      - "8080:8080"
    environment:
      SERVER_PORT: 8080
      DB_URL: jdbc:h2:mem:productsdb
      DB_USER: sa
      DB_PASSWORD:
    restart: unless-stopped